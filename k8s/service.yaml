apiVersion: v1
kind: Service
metadata:
  name: serper-search-service
  namespace: mcp-servers
  labels:
    app: serper-search
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: serper-search
