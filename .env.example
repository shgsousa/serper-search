# Serper API Configuration
SERPER_API_KEY=your_serper_api_key_here

# Optional: Rate limiting configuration (default: 500ms)
SEARCH_RATE_LIMIT_MS=500

# Optional: HTTP server port (default: 3000)
PORT=3000

# Optional: Enable HTTP mode instead of stdio
MCP_HTTP_MODE=false

# For Docker deployment:
# 1. Copy this file to .env.docker
# 2. Set your actual SERPER_API_KEY value
# 3. Run: docker-compose up
